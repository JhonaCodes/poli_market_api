# ========================================
# POLIMARKET API - Environment Variables
# ========================================
# Copy this file to .env and configure with your values
# For Docker/Dokploy deployment, these should be set in your container environment

# ----------------------------------------
# DATABASE CONFIGURATION
# ----------------------------------------
# PostgreSQL connection string
# Format: postgres://username:password@hostname:port/database
#
# Local development:
DATABASE_URL=postgres://postgres:postgres@localhost:5432/polimarket
#
# Docker Compose (service name is 'postgres'):
# DATABASE_URL=postgres://postgres:postgres@postgres:5432/polimarket
#
# Dokploy/Production (use your actual database host):
# DATABASE_URL=postgres://user:password@your-db-host:5432/polimarket

# ----------------------------------------
# SERVER CONFIGURATION
# ----------------------------------------
# Server bind address
# Use 0.0.0.0 to accept connections from any network interface
# Use 127.0.0.1 for localhost only (not suitable for Docker)
SERVER_HOST=0.0.0.0

# Server port
# Make sure this matches your Docker EXPOSE and port mapping
SERVER_PORT=8080

# ----------------------------------------
# LOGGING CONFIGURATION
# ----------------------------------------
# Rust logging level
# Options: error, warn, info, debug, trace
RUST_LOG=info

# Enable backtrace on errors (useful for debugging)
# Options: 0 (disabled), 1 (enabled), full (full backtrace)
# RUST_BACKTRACE=1

# ----------------------------------------
# DATABASE CONNECTION POOL
# ----------------------------------------
# Maximum number of connections in the pool
# Adjust based on your expected load and database limits
POOL_MAX_SIZE=10

# Minimum number of idle connections to maintain
# Lower values save resources, higher values improve response time
POOL_MIN_IDLE=2

# Connection timeout in seconds
# How long to wait for a connection from the pool
POOL_TIMEOUT_SECONDS=30

# ----------------------------------------
# NOTES FOR DEPLOYMENT
# ----------------------------------------
#
# DOCKER COMPOSE:
# - The DATABASE_URL host should be 'postgres' (the service name)
# - All environment variables are already configured in docker-compose.yml
#
# DOKPLOY:
# - Set these variables in your Dokploy environment configuration
# - Make sure DATABASE_URL points to your managed database instance
# - Use strong passwords in production
# - Enable HTTPS/TLS for database connections if available
#
# DEVELOPMENT:
# - Use .env file for local development
# - Never commit .env files with real credentials
# - Use .env.local for machine-specific overrides
